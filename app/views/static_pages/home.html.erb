<div id = "container-fluid">
  <div class="center hero-unit">
    <h1>Find a good place to stay in</h1>
  	<div id = "search-bar">
  		<%= form_tag(search_path, :method => 'get') do %>
  		<%= text_field_tag(:search) %>
  		<%= submit_tag("Search" , :name => nil, :class => "search-submit btn") %>
  		<% end %>
  	</div>
  </div>
</div>