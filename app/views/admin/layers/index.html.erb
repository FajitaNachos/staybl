<h1>Listing admin_layers</h1>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @layers.each do |layer| %>
  <tr>
    <td><%= link_to 'Show', ([:admin, layer]) %></td>
    <td><%= link_to 'Edit', edit_admin_layer_path(layer) %></td>
    <td><%= link_to 'Destroy', [:admin, layer], method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Layer', new_admin_layer_path %>
